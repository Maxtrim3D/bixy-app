pluginManagement {
    includeBuild("../node_modules/@react-native/gradle-plugin")
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

// NOTE: ReactSettingsPlugin does not exist in @react-native/gradle-plugin@0.73.4.
// Repositories are configured via allprojects{} in build.gradle (classic RN pattern).
// com.facebook.react is excluded from mavenCentral() to force resolution from node_modules.

rootProject.name = 'BixyApp'
include ':app'
includeBuild('../node_modules/@react-native/gradle-plugin')
